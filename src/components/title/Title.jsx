import React from "react";

const Title = ({ text }) => {
  return (
    <h2 className="text-purple-800 font-black font-Poppins text-2xl text-center">
      {text}
    </h2>
  );
};

export default Title;
